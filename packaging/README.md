# Packaging helpers

This folder contains optional packaging helpers.

- Windows MSI: use cargo-wix (requires the WiX Toolset). [web:303]
- CI releases: cargo-dist can generate cross-platform release pipelines. [web:301]
